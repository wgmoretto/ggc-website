{% extends "layout.twig" %}

{% block title %}Master Level Rankings - {{ server_name }}{% endblock %}

{% block content %}
<h1><i class="fa fa-star"></i> Top Master Level Players</h1>

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">Master Level Rankings - Top 100</h3>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Character</th>
                    <th>Class</th>
                    <th>Master Level</th>
                    <th>Level</th>
                    <th>Resets</th>
                </tr>
            </thead>
            <tbody>
            {% for player in rankings %}
                <tr>
                    <td>{{ loop.index }}</td>
                    <td><a href="/profile/player/{{ player.Name }}">{{ player.Name }}</a></td>
                    <td>{{ player.ClassName }}</td>
                    <td><span class="label label-warning">{{ player.MasterLevel }}</span></td>
                    <td><span class="label label-success">{{ player.cLevel }}</span></td>
                    <td><span class="label label-info">{{ player.Resets|default(0) }}</span></td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-center">No rankings available</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<a href="/rankings" class="btn btn-default">
    <i class="fa fa-arrow-left"></i> Back to Rankings
</a>
{% endblock %}
