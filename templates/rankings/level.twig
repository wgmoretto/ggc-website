{% extends "layout.twig" %}

{% block title %}Level Rankings - {{ server_name }}{% endblock %}

{% block content %}
<h1><i class="fa fa-trophy"></i> Top Level Players</h1>

<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title">Level Rankings - Top 100</h3>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Character</th>
                    <th>Class</th>
                    <th>Level</th>
                    <th>Resets</th>
                    <th>Map</th>
                </tr>
            </thead>
            <tbody>
            {% for player in rankings %}
                <tr>
                    <td>
                        {% if loop.index == 1 %}
                            <i class="fa fa-trophy" style="color: gold;"></i>
                        {% elseif loop.index == 2 %}
                            <i class="fa fa-trophy" style="color: silver;"></i>
                        {% elseif loop.index == 3 %}
                            <i class="fa fa-trophy" style="color: #CD7F32;"></i>
                        {% else %}
                            {{ loop.index }}
                        {% endif %}
                    </td>
                    <td><a href="/profile/player/{{ player.Name }}">{{ player.Name }}</a></td>
                    <td>{{ player.ClassName }}</td>
                    <td><span class="label label-success">{{ player.cLevel }}</span></td>
                    <td><span class="label label-info">{{ player.Resets|default(0) }}</span></td>
                    <td>{{ player.MapNumber }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6" class="text-center">No rankings available</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<a href="/rankings" class="btn btn-default">
    <i class="fa fa-arrow-left"></i> Back to Rankings
</a>
{% endblock %}
